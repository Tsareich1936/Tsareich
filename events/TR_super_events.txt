add_namespace = TR_super_event

country_event = {#Test event
	id = TR_super_event.0
	hidden = yes
	is_triggered_only = yes

	immediate = {
		set_variable = {
			# TR_super_event_variable = 0 # event id
			TR_showing_super_event = 1 # 0:unvisible 1:visible
		}
	}
}
country_event = {#One Italy, One Nation
	id = TR_super_event.1
	hidden = yes
	is_triggered_only = yes
	trigger = {
	}

	immediate = {
		set_variable = {
			TR_super_event_variable = 1 # event id
		}
		set_variable = {
			TR_showing_super_event = 1 # 0:unvisible 1:visible
		}
		hidden_effect = {
			play_song = "risorgimento_italiano"
		}
	}
}

country_event = {#American Civil War
	id = TR_super_event.2
	hidden = yes
	is_triggered_only = yes
	trigger = {
	}

	immediate = {
		set_variable = {
			TR_super_event_variable = 2 # event id
		}
		set_variable = {
			TR_showing_super_event = 1 # 0:unvisible 1:visible
		}
		hidden_effect = {
			play_song = "when_johnny_comes_marching_home"
		}
	}
}

country_event = {
	id = TR_super_event.3
	hidden = yes
	fire_only_once = yes
	is_triggered_only = yes
	trigger = {
	}
	immediate = {
		set_variable = {
			TR_super_event_variable = 3
		}
		set_variable = {
			TR_showing_super_event = 1
		}
		hidden_effect = {
			play_song = "Army_Detachment_March"
		}
	}
}
country_event = {#German Civil War
	id = TR_super_event.4
	hidden = yes
	fire_only_once = yes
	is_triggered_only = yes
	trigger = {
	}
	immediate = {
		set_variable = {
			TR_super_event_variable = 4
		}
		set_variable = {
			TR_showing_super_event = 1
		}
		hidden_effect = {
			play_song = "einheitsfrontlied"
		}
	}
}
country_event = {#Tsar War
	id = TR_super_event.5
	hidden = yes
	fire_only_once = yes
	is_triggered_only = yes
	trigger = {
	}
	immediate = {
		set_variable = {
			TR_super_event_variable = 5
		}
		set_variable = {
			TR_showing_super_event = 1
		}
	}
}
country_event = {#Unified America
	id = TR_super_event.6
	hidden = yes
	fire_only_once = yes
	is_triggered_only = yes
	trigger = {
	}
	immediate = {
		set_variable = {
			TR_super_event_variable = 6
		}
		set_variable = {
			TR_showing_super_event = 1
		}
		hidden_effect = {
			play_song = "The_Star_Spangled_Banner"
		}
	}
}