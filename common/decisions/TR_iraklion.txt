TR_iraklion_conference = {
	TR_IC_mission = {
		icon = Conference
		allowed = {
			tag = GRE
		}
		days_mission_timeout = 14
		is_good = yes
		fire_only_once = yes

		cost = 0

		timeout_effect = {
			set_global_flag = TR_iraklion_finished_flag
			clr_global_flag = TR_iraklion_started_flag

			for_each_loop = {
				array = iraklion_members
				if = {
					limit = {
						has_global_flag = finished_soc
						has_global_flag = finished_safe
						has_global_flag = finished_tur
					}
					country_event = {# Iraklion Conference Failed

					}
				}
				else = {
					country_event = {# Iraklion Conference Failed

					}
				}
			}
		}
	}
	TR_IC_poverty_issue_socialist = {
		icon = communism
		allowed = {
			tag = GRE
		}
		available = {
			NOT = { has_country_flag = TR_IC_progressing }
		}
		fire_only_once = yes

		cost = 0

		ai_will_do = {
			base = 100
			factor = 1
		}

		complete_effect = {
			set_country_flag = TR_IC_progressing
			country_event = {
				id = TR_iraklion.12
			}
		}
	}
	TR_IC_poverty_issue_security_guarantee = {
		icon = Military
		allowed = {
			tag = GRE
		}
		available = {
			NOT = { has_country_flag = TR_IC_progressing }
		}
		fire_only_once = yes

		ai_will_do = {
			base = 100
			factor = 0.9
		}

		complete_effect = {
			set_country_flag = TR_IC_progressing
			for_each_loop = {
				array = iraklion_members
				country_event = {
					
				}
			}
		}
	}
	TR_IC_poverty_issue_turkey = {
		icon = TUR
		allowed = {
			tag = GRE
		}
		available = {
			NOT = { has_country_flag = TR_IC_progressing }
		}
		fire_only_once = yes

		ai_will_do = {
			base = 100
			factor = 0.8
		}

		complete_effect = {
			set_country_flag = TR_IC_progressing
			for_each_loop = {
				array = iraklion_members
				country_event = {
					
				}
			}
		}
	}
}